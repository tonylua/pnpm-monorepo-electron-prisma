{
  "name": "@app/common",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/types.d.ts",
  "scripts": {
    "build": "pnpm clean && pnpm db:generate && node build.mjs",
    "clean": "rimraf dist",
    "db:migrate": "prisma db update --confirm myDb.db",
    "db:generate": "prisma contract emit",
    "test": "node --test"
  },
  "dependencies": {
    "@prisma/orm-sqlite": "catalog:@prisma/orm-sqlite",
    "uuid": "^11.0.5"
  },
  "devDependencies": {
    "@prisma/cli-engine": "catalog:@prisma/cli-engine",
    "dotenv": "^16.4.7",
    "dotenv-cli": "^8.0.0",
    "esbuild": "^0.25.0",
    "fast-check": "^4.8.0",
    "prisma": "catalog:prisma",
    "rimraf": "^5.0.0",
    "typescript": "^5.3.3"
  }
}
